# PROMPTS.md (AI Usage)

Below are the prompts used while building this project:

1. "Build a 4-step multi-page enrollment flow in Next.js App Router + TypeScript using react-hook-form + zod + shadcn/ui."

2. "Create step-wise zod validation schemas for step-1, step-2, step-3 and a merged schema for final submit validation."

3. "Make Step 2 subjects multi-select using shadcn Popover + Command and show selected subjects as chips."

4. "Add conditional validation using zod.superRefine (scholarship toggle requires lastExamPercentage)."

5. "Add route guards so user cannot open later steps without completing previous steps."

6. "Implement localStorage draft persistence in a layout-level React Context and autosave every 2 seconds (debounced)."

7. "Create Step 3 PIN auto-fill mock feature: when PIN is valid, autofill state and city using a local map."

8. "Create Review page with read-only summary, Edit buttons, final validation, 1-second fake submit, success alert, and collapsible JSON payload."

9. "Fix TypeScript issues related to resolver types, uncontrolled-to-controlled input warnings, and zod schema refinement merging."
